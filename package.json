{
  "dependencies": {
    "normalize.css": "^5.0.0"
  },
  "devDependencies": {
    "cssnano": "^3.8.1",
    "postcss": "^5.2.6",
    "postcss-cli": "^2.6.0",
    "postcss-cssnext": "^2.8.0",
    "postcss-import": "^8.2.0",
    "stylelint": "^7.6.0",
    "stylelint-config-standard": "^15.0.0"
  },
  "scripts": {
    "build": "postcss -c postcss.config.json",
    "lint": "stylelint style.css",
    "watch": "postcss -c postcss.config.json --watch"
  },
  "stylelint": {
    "extends": "stylelint-config-standard"
  }
}
